<!-- 1. Переменные типа число в javascript могут содержать

только целые числа?
как целые так и с плавающей точкой? *
только положит?
только числа с плавающей? 
только целые ?

2. Как называется область видимости в JavaScript, которая позволяет объявлять переменные, доступны только внутри блока, где они были определены?

Область видимости объекта
Блочная область видимости *
Область видимости цикла
Глобальная область видимости
Функциональная область видимости

3. Перед вами поставили задачу написать веб-приложение «Крестики-нолики». По техническому заданию вам необходимо сохранять состояние игры. Например, вы можете сохранять текущего игрока, расположение крестиков и ноликов на игровом поле и другие данные, чтобы игра могла быть возобновлена после перезагрузки страницы.

Какой тип хранилища вы подберете?

Атрибуты форм (Form Attributes)
Куки (cookies)
SessionStorage
IndexedDB
LocalStorage *

4. Чему будет равен z после выполнения следующего кода?

javascript
const x = 5;
const y = 2;
const z = x % y;

0
2.5
1  *
3
2

5. какой оператор используется для логического И:

&& *

6. Что нужно вставить на место пропуска, чтобы выражение ниже возвращало true с учетом типа данных?

javascript
const question = 5 ___ '5';
!==  *
=
<>
==
==

7. javascript
const x = 10;
if (x > 5 && x < 15) {
    console.log("x находится в диапазоне от 5 до 15");
} else {
    console.log("x не находится в диапазоне от 5 до 15");
}
Зависит от значения переменной x
В консоль будет выведено «x находится в диапазоне от 5 до 15»  *
В консоль будет выведено «x не находится в диапазоне от 5 до 15»
Ничего не произойдет, так как код содержит ошибку
Будет выброшена ошибка синтаксиса

8. У вас есть задача, в которой:
— Важно, чтобы при любых обстоятельствах блок кода выполнялся минимум один раз
— Есть динамические условия, которые могут изменяться в ходе выполнения блока кода
— Количество итераций заранее неизвестно
Какой тип оператора оптимально использовать для решения этой задачи?

for
do...while  *
if else
оператор ?
рекурсия

9. Какой из перечисленных вариантов объявления функции с параметрами является верным?

function myFunction() {}
function myFunction(parameters) {}  *
function myFunction = () => parameters
function myFunction(parameters)
function myFunction {parameters}

10. Какое значение будет выведено в консоль при выполнении кода?

javascript
function outerFunction() {
    const x = 10;
    function innerFunction() {
        const y = 20;
        console.log(x + y);
    }
    innerFunction();
}
outerFunction();
10
20
30  *
40
Ошибка: переменная не определена -->